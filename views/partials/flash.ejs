<% if (flash && flash.success) { %>
  <div class="flash flash-success"><%= decodeURIComponent(flash.success) %></div>
<% } %>
<% if (flash && flash.error) { %>
  <div class="flash flash-error"><%= decodeURIComponent(flash.error) %></div>
<% } %>

